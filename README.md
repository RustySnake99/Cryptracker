# Cryptracker
A web application made using the Python - FastAPI framework that uses the CoinGecko API to fetch real time closing prices of entered cryptocurrencies like Bitcoin (BTC). Displays details, graphs etc.
To run the app, use the command: `uvicorn main:app --reload`
